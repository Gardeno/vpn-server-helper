[uwsgi]

# Django-related settings
# the base directory (full path)
chdir = /home/ubuntu/tunnel-server
module = main:app
home = /home/ubuntu/tunnel-server/venv
touch-reload = /home/ubuntu/tunnel-server/reload

# process-related settings
# master
master = true
# maximum number of worker processes
processes = 10
# the socket (use the full path to be safe
socket = /tmp/tunnel.gardeno.sock
# ... with appropriate permissions - may be needed
chmod-socket = 666
# clear environment on exit
vacuum = true